name: Preview Quarto Book

on:
  workflow_dispatch:
  push:
    branches: 
      - copilot/convert-bookdown-to-quarto

jobs:
  build-preview:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      # Checkout repository
      - uses: actions/checkout@v4

      # Setup Quarto
      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      # Setup R environment
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      # Install system libraries for the V8 R package
      - name: Install system libraries for V8
        run: |
          sudo apt-get update
          # Try libv8-dev first; if not available on this Ubuntu image, fall back to libnode-dev
          sudo apt-get install -y libv8-dev || sudo apt-get install -y libnode-dev
          # ICU is a common runtime dependency for V8 on Ubuntu
          sudo apt-get install -y libicu-dev

      # Setup renv if needed
      - uses: r-lib/actions/setup-renv@v2

      # Ensure _quarto directory exists
      - name: Ensure _quarto directory exists
        run: mkdir -p _quarto

      # Cache quarto results
      - name: Cache quarto results
        uses: actions/cache@v4
        with:
          path: _quarto
          key: quarto-preview-${{ hashFiles('**/*qmd') }}
          restore-keys: quarto-preview-

      # Render Quarto book
      - name: Render Quarto Book
        run: quarto render

      # Deploy to preview branch on gh-pages
      - name: Deploy preview to gh-pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4.6.4
        with:
          branch: gh-pages
          folder: docs
          target-folder: preview/quarto-conversion
          clean: false

      # Comment on PR with preview link
      - name: Comment Preview Link
        uses: actions/github-script@v7
        if: github.event_name == 'push'
        with:
          script: |
            const previewUrl = 'https://moderndive.github.io/ModernDive_book/preview/quarto-conversion/';
            const message = `## 📚 Quarto Book Preview Available!
            
            Your Quarto book preview has been deployed and is ready to explore:
            
            🔗 **[View Full Quarto Book Preview](${previewUrl})**
            
            This preview includes:
            - ✅ Full book rendering with all R code executed
            - ✅ All 11 chapters + appendices
            - ✅ Interactive navigation and search
            - ✅ All figures, tables, and outputs
            - ✅ Complete styling and formatting
            
            The preview is deployed at: \`${previewUrl}\`
            
            _Preview automatically updates on new commits to this branch._`;
            
            console.log('Preview deployed to:', previewUrl);
            console.log(message);
