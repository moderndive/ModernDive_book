Solutions for the (short) end-of-chapter exercises in *Chapter 1 — Getting Started with Data in R*. These prompts focus on inspecting `olympic_athletes` from `olympicAthletes` — a gentle on-ramp to the larger exercise sets in *Chapter 2* onward.

```{r}
#| label: ch1-coverage
#| results: asis
#| echo: false
#| message: false
helpers <- if (file.exists("scripts/exercise_helpers.R")) "scripts/exercise_helpers.R" else "../scripts/exercise_helpers.R"
source(helpers)
cat(render_coverage_callout(1))
```

```{r}
#| label: 01-ex-setup
#| message: false
library(dplyr)
library(olympicAthletes)
```

```{r}
#| label: ch1-solutions
#| results: asis
#| echo: false
#| message: false
cat(render_solutions(1))
```
