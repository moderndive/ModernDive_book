Solutions for the end-of-chapter exercises in *Chapter 8 — Confidence Intervals*. Datasets: `volcanoes`, `episodes` (`steves`), `olympic_athletes`, plus `bob_ross` (`fivethirtyeight`).

```{r}
#| label: ch8-coverage
#| results: asis
#| echo: false
#| message: false
helpers <- Find(file.exists, c("scripts/exercise_helpers.R", "../scripts/exercise_helpers.R", "../../scripts/exercise_helpers.R"))
source(helpers)
cat(render_coverage_callout(8))
```

```{r}
#| label: 08-ex-setup
#| message: false
library(dplyr)
library(ggplot2)
library(infer)
library(moderndive)
library(volcanoes)
library(steves)
library(olympicAthletes)
library(fivethirtyeight)

set.seed(76)
samp <- volcanoes |> rep_slice_sample(n = 50) |> ungroup()
mu   <- mean(volcanoes$elevation, na.rm = TRUE)
```

```{r}
#| label: ch8-solutions
#| results: asis
#| echo: false
#| message: false
cat(render_solutions(8))
```
