Solutions for the end-of-chapter exercises in *Chapter 6 — Multiple Regression*. Dataset: `planets` from [`exoplanets`](https://github.com/moderndive/exoplanets).

```{r}
#| label: ch6-coverage
#| results: asis
#| echo: false
#| message: false
helpers <- Find(file.exists, c("scripts/exercise_helpers.R", "../scripts/exercise_helpers.R", "../../scripts/exercise_helpers.R"))
source(helpers)
cat(render_coverage_callout(6))
```

```{r}
#| label: 06-ex-setup
#| message: false
library(dplyr)
library(ggplot2)
library(moderndive)
library(exoplanets)

planets_lite <- planets |>
  filter(!is.na(radius_earth), !is.na(mass_earth),
         !is.na(discovery_method), !is.na(eq_temp_k)) |>
  mutate(log_mass = log10(mass_earth),
         log_radius = log10(radius_earth))

top2 <- planets_lite |> count(discovery_method, sort = TRUE) |>
  slice_head(n = 2) |> pull(discovery_method)
pl2 <- planets_lite |> filter(discovery_method %in% top2)

planets_temp <- planets |>
  filter(!is.na(eq_temp_k), !is.na(star_temp_k), !is.na(insolation_earth)) |>
  mutate(log_insol = log10(insolation_earth))
```

```{r}
#| label: ch6-solutions
#| results: asis
#| echo: false
#| message: false
cat(render_solutions(6))
```
