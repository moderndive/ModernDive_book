Solutions for the end-of-chapter exercises in *Chapter 10 — Inference for Regression*. Datasets: `olympic_athletes` (`olympicAthletes`) and `planets` (`exoplanets`).

```{r}
#| label: ch10-coverage
#| results: asis
#| echo: false
#| message: false
helpers <- if (file.exists("scripts/exercise_helpers.R")) "scripts/exercise_helpers.R" else "../scripts/exercise_helpers.R"
source(helpers)
cat(render_coverage_callout(10))
```

```{r}
#| label: 10-ex-setup
#| message: false
library(dplyr)
library(ggplot2)
library(infer)
library(moderndive)
library(broom)
library(olympicAthletes)
library(exoplanets)

bball <- olympic_athletes |>
  filter(sport == "Basketball", !is.na(height), !is.na(weight))
m_b <- lm(weight ~ height, data = bball)

planets_lite <- planets |>
  filter(!is.na(radius_earth), !is.na(mass_earth), !is.na(discovery_method)) |>
  mutate(log_mass = log10(mass_earth), log_radius = log10(radius_earth))
top2 <- planets_lite |> count(discovery_method, sort = TRUE) |>
  slice_head(n = 2) |> pull(discovery_method)
pl2 <- planets_lite |> filter(discovery_method %in% top2)
```

```{r}
#| label: ch10-solutions
#| results: asis
#| echo: false
#| message: false
cat(render_solutions(10))
```
