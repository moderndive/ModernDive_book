Solutions for the end-of-chapter exercises in *Chapter 3 — Data Wrangling*. Datasets: `olympic_athletes` and `medal_table` from [`olympicAthletes`](https://github.com/moderndive/olympicAthletes), and `episodes` from [`steves`](https://github.com/ismayc/steves) — a tidied snapshot of every *Rick Steves' Europe* episode (159 rows × 38 columns).

```{r}
#| label: ch3-coverage
#| results: asis
#| echo: false
#| message: false
helpers <- if (file.exists("scripts/exercise_helpers.R")) "scripts/exercise_helpers.R" else "../scripts/exercise_helpers.R"
source(helpers)
cat(render_coverage_callout(3))
```

```{r}
#| label: 03-ex-setup
#| message: false
library(dplyr)
library(ggplot2)
library(olympicAthletes)
library(steves)
```

```{r}
#| label: ch3-solutions
#| results: asis
#| echo: false
#| message: false
cat(render_solutions(3))
```
